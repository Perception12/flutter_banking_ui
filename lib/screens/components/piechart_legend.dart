import 'package:flutter/material.dart';
import '../../colors.dart' as color;

class PieChartLegend extends StatelessWidget {
  const PieChartLegend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
