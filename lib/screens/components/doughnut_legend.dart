import 'package:flutter/material.dart';
import '../../colors.dart' as color;

class DoughnutLegend extends StatelessWidget {
  const DoughnutLegend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
